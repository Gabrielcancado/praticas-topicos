#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    float s, n=1, pi, f;
    while (1/(pow(n,3)) > 0.0001){
    s = s + (1/(pow(n,3)));
    n = n+ 2;
    }
    f = s * 32;
    cout << f;
}
